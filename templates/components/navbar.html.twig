{# Simple standard navbar — replaces the old circular navigation wheel #}
<nav class="site-navbar" id="siteNavbar">
    <a href="{{ path('app_home') }}" class="site-navbar__brand">
        <img src="{{ asset('images/panneaux-solaires-sous-la-pluie.png') }}" alt="Sirius-Solar" class="site-navbar__brand-icon">
        <span>Sirius<span class="site-navbar__brand-accent">Solar</span></span>
    </a>

    <ul class="site-navbar__links">
        <li><a href="{{ path('app_home') }}">Accueil</a></li>
        <li><a href="#processus">Processus</a></li>
        <li><a href="#services">Services</a></li>
        <li><a href="#avantages">Avantages</a></li>
        <li><a href="#contact" onclick="window.runContactScramble && window.runContactScramble()">Contact</a></li>
        <li><a href="/about">À propos</a></li>
        <li><a href="/blog">Blog</a></li>
    </ul>
</nav>

<style>
    .site-navbar {
        position: fixed;
        top: 0;
        left: 0;
        right: 0;
        z-index: 900;
        display: flex;
        align-items: center;
        gap: 36px;
        padding: 16px 220px 16px 32px;
        background: linear-gradient(180deg, rgba(2, 20, 16, 0.55) 0%, rgba(2, 20, 16, 0) 100%);
    }

    .site-navbar__brand {
        display: flex;
        align-items: center;
        gap: 10px;
        text-decoration: none;
        font-weight: 700;
        font-size: 18px;
        color: #00ff9d;
        letter-spacing: 0.01em;
        white-space: nowrap;
        font-family: 'Space Grotesk', sans-serif;
        text-shadow: 0 0 12px rgba(0, 255, 157, 0.4);
    }

    .site-navbar__brand-icon {
        width: 32px;
        height: 32px;
        border-radius: 8px;
        object-fit: cover;
        border: 1px solid rgba(255, 255, 255, 0.3);
    }

    .site-navbar__brand-accent {
        color: #00ff9d;
    }

    .site-navbar__links {
        display: flex;
        align-items: center;
        gap: 26px;
        list-style: none;
        margin: 0;
        padding: 0;
        flex-wrap: wrap;
    }

    .site-navbar__links a {
        position: relative;
        color: #00ff9d;
        text-decoration: none;
        font-size: 14px;
        font-weight: 500;
        padding: 4px 0;
        transition: color 0.2s ease, text-shadow 0.2s ease;
    }

    .site-navbar__links a::after {
        content: '';
        position: absolute;
        left: 0;
        bottom: -2px;
        width: 0;
        height: 1px;
        background: #00ff9d;
        transition: width 0.2s ease;
    }

    .site-navbar__links a:hover {
        color: #00ff9d;
        text-shadow: 0 0 10px rgba(0, 255, 157, 0.6);
    }

    .site-navbar__links a:hover::after {
        width: 100%;
    }

    @media (max-width: 1100px) {
        .site-navbar {
            padding-right: 32px;
        }
        .site-navbar__links {
            gap: 18px;
        }
    }

    @media (max-width: 768px) {
        .site-navbar {
            display: none;
        }
    }
</style>
